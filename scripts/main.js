$(document).ready(function() {
    const grid = new Grid('#board', 12, 12);
    const specialSpots = new SpecialBlocks(30, 4);
    const player1setup = new Player('player1icon', 100, 10);
    const player2setup = new Player('player2icon', 100, 10);
});
